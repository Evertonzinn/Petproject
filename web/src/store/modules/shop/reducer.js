const INITIAL_STATE = {
    customer: {},
};

function shop (state = INITIAL_STATE, action) {
    switch (action.type) {
        case 'SET_CUSTOMER': {
            
        }


        default:
        return state;
    }
}
export default shop;